<Window x:Class="FishRestaurant.WPF.Cash"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cash" Height="300" Width="300">
    <!--<Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Frame Grid.Row="2" NavigationUIVisibility="Hidden" Name="Frame" Navigated="Frame_Navigated"/>
        <Border BorderBrush="Black" BorderThickness="3" Padding="0,2">
            <Grid Name="BTN_GD">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1" Style="{StaticResource Tabs}" Click="Sell_BTN_Click" Content="البيع" Background="#FF323B3B" Name="Sell_BTN"/>
                <Button Grid.Column="0" Style="{StaticResource Tabs}" Click="Sell_Back_BTN_Click" Content="مرتجع البيع" Background="#FF323B3B" Name="Sell_Back_BTN" />
            </Grid>
        </Border>
        
    </Grid>-->
</Window>
